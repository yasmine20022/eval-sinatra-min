apiVersion: apps/v1
kind: Deployment
metadata:
  name: eval-sinatra-min
  labels:
    app.kubernetes.io/name: eval-sinatra-min
    app.kubernetes.io/component: web
    app.kubernetes.io/part-of: eval-sinatra-min
    app.kubernetes.io/managed-by: helm
spec:
  replicas: 1
  selector:
    matchLabels:
      app: eval-sinatra-min
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
  template:
    metadata:
      labels:
        app: eval-sinatra-min
        app.kubernetes.io/name: eval-sinatra-min
        app.kubernetes.io/component: web
        app.kubernetes.io/part-of: eval-sinatra-min
        app.kubernetes.io/managed-by: helm
    spec:
      containers:
      - name: eval-sinatra-min
        image: eval-sinatra-min:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 8000
        resources:
          requests:
            cpu: 250m
            memory: 256Mi
          limits:
            cpu: 500m
            memory: 512Mi
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 15
          periodSeconds: 10
          timeoutSeconds: 2
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 10
          timeoutSeconds: 2
          failureThreshold: 3
        startupProbe:
          httpGet:
            path: /health
            port: 8000
          periodSeconds: 10
          failureThreshold: 30
